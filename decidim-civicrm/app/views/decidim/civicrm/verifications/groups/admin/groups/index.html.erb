<p class="callout warning">
  <%= t(".warning_1") %>
  <br>
  <%= t(".warning_2") %>
</p>
<div class="card">
  <div class="card-divider">
    <h2 class="card-title">
      <%= t(".title") %>

      <div class="button--title">
        <%= link_to t(".participatory_process_group_assignments"), participatory_process_group_assignments_path, class: "button tiny" %>
      </div>
    </h2>
  </div>
  <div class="card-section">
    <% if groups.present? %>
      <table class="table-list civicrm-groups">
        <thead>
          <tr>
            <th><%= t(".group_title") %></th>
            <th><%= t(".description") %></th>
            <th><%= t(".visibility") %></th>
            <th><%= t(".count") %></th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% groups.each do |group| %>
            <tr>
              <td><%= group[:title] %></td>
              <td><%= group[:description] %></td>
              <td><%= group[:visibility] %></td>
              <td><%= group[:count] %></td>
              <td><%= icon_link_to "reload", group_path(id: group[:id], name: group[:name], title: group[:title]), t(".update"), method: :post %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% else %>
      <p class="alert"><%= t(".error") %></p>
    <% end %>
  </div>
</div>
